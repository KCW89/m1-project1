/**
 * ResetButton Component (TODO #5)
 *
 * This component provides a button to reset the counter to zero.
 *
 * Your Tasks:
 * 1. Import necessary React Native components
 * 2. Accept 'onPress' as a prop
 * 3. Create a TouchableOpacity button
 * 4. Style it with a danger/red color
 * 5. Ensure 44px minimum touch target
 *
 * Expected Props:
 * - onPress: Function (called when button is pressed)
 *
 * Hints:
 * - Use TouchableOpacity for the button
 * - Use colors.danger for background color
 * - Add white text with "Reset" label
 * - Set minHeight: 44 for touch target
 * - Use borderRadius: 8 for rounded corners
 */

import React from 'react';
// TODO: Import TouchableOpacity, Text, StyleSheet from 'react-native'
// TODO: Import colors from '../styles/colors'

// TODO: Complete this component
export default function ResetButton({ onPress }) {
  // Your code here
  // Return a TouchableOpacity with:
  // - onPress prop passed through
  // - Text saying "Reset"
  // - Proper styling (red background, white text)

  return null; // Replace with your implementation
}

// TODO: Create StyleSheet with:
// - button: danger color background, padding, minHeight 44px, centered content
// - text: white color, bold font
