/**
 * CounterDisplay Component (TODO #4)
 *
 * This component displays the current count with large, readable text.
 *
 * Your Tasks:
 * 1. Import necessary React Native components
 * 2. Accept 'count' and 'color' as props
 * 3. Display the count with styling
 * 4. Use the color prop to change text color
 *
 * Expected Props:
 * - count: Number (the current counter value)
 * - color: String (optional, for visual feedback)
 *
 * Hints:
 * - Use View for container
 * - Use Text to display the count
 * - Import colors from '../styles/colors'
 * - Make the count text large (fontSize: 64)
 * - Add a label above the count ("Count")
 */

import React from 'react';
// TODO: Import View, Text, StyleSheet from 'react-native'
// TODO: Import colors from '../styles/colors'

// TODO: Complete this component
export default function CounterDisplay({ count, color }) {
  // Your code here
  // Return a View with:
  // - A label Text ("Count")
  // - A count Text showing the count value
  // - Proper styling

  return null; // Replace with your implementation
}

// TODO: Create StyleSheet with:
// - container: centered with margin
// - label: small text for "Count"
// - count: large text (64px) for the number
